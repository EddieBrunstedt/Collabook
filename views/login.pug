extends layout

block content
  form.form-signin.text-center(action='/login', method='post')
    i.fas.fa-user.fa-2x.mb-3.text-muted
    h1.h3.mb-3.font-weight-normal Sign in
    label.sr-only(for='inputEmail') Email address
    input#inputEmail.form-control(type='email', placeholder='Email address', required='', autofocus='', name="inputEmail", value="eddie.brunstedt@gmail.com")
    label.sr-only(for='inputPassword') Password
    input#inputPassword.form-control(type='password', placeholder='Password', required='', name="inputPassword", value="hej")
    button.btn.btn-lg.btn-dark.btn-block(type='submit') Submit
    p.mt-5.mb-3.text-muted © 2018-2019