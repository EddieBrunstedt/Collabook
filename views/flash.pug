if success_msg.length > 0
  each val in success_msg
    .alert-success
      | #{val}
if error_msg.length > 0
  each val in error_msg
    .alert-danger
      | #{val}
if error.length > 0
  .alert-danger
    | #{error}
if success.length > 0
  .alert-success
    | #{success}